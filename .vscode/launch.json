{
    "version": "0.2.0",
    "configurations": [
        {
            "type": "lldb",
            "request": "launch",
            "name": "Debug",
            "program": "${workspaceFolder}/main",
            "args": ["./src/tail.c", "-n", "20"],
            "cwd": "${workspaceFolder}",
            "preLaunchTask": "Makefile",
            "env": {
                "ASAN_OPTIONS": "detect_leaks=0"
            }
        }
    ]
}
